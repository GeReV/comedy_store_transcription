[project]
name = "comedy-store-transcribe"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = ["PyQt6"]

[project.scripts]
player = "scripts.player:main"

[dependency-groups]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
